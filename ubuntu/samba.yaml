metadata:
    name: samba
    format: "Lava-Test-Shell Test Definition 1.0"
    description: "Run tests from sambe suite on Ubuntu"
    maintainer:
        - wu.wu@hisilicon.com
    os:
        - ubuntu
    scope:
        - functional
    devices:
        - d02
        - d01
        - hikey
    environment:
        - lava-test-shell

install:
    deps:
        - samba

run:
    steps:
        - "./ubuntu/scripts/samba.sh"

